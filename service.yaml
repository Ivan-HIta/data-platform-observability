apiVersion: v1
kind: Service
metadata:
  name: data-platform-dashboard
spec:
  selector:
    app: data-platform-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
