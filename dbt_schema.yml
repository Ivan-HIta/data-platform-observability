version: 2

models:
  - name: gold_service_daily
    description: Daily operational service metrics published for analytics.
    columns:
      - name: event_date
        tests: [not_null]
      - name: region
        tests: [not_null]
      - name: events
        tests: [not_null]

