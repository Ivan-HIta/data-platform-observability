name: "data_platform_observability"
version: "1.0.0"
config-version: 2

profile: "data_platform_observability"

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  data_platform_observability:
    staging:
      +materialized: view
    marts:
      +materialized: table
